// IPC Handler registration placeholder
// Handlers will be registered here as features are implemented
export function registerIpcHandlers(): void {
  // Thin dispatch layer — business logic lives in services/
}
